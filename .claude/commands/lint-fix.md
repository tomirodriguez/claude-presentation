---
description: Run linter and fix issues
allowed-tools: Bash, Read, Edit
---

Run `pnpm run lint:fix` to auto-fix lint issues. If there are errors that can't be auto-fixed, fix them manually and run the command again until all errors are resolved.
